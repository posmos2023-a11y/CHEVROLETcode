# Local/generated artifacts
*.zip
node_modules/

# Local secrets and database files (backend/.gitignore also covers these)
.env
backend/.env
backend/prisma/dev.db
backend/prisma/dev.db-journal

# ERP 시뮬레이터 설정 파일 - ERP 토큰(비밀값)이 들어가므로 커밋 금지
erp-simulator/config.ini

# 파이썬 바이트코드 캐시. 앱을 한 번 실행하기만 해도 생겨서, 없으면 매번 커밋 대상으로 뜬다.
__pycache__/
*.pyc

# PyInstaller 산출물. exe 하나가 30MB가 넘어서 저장소에 넣을 물건이 아니다 — build.ps1로 언제든
# 다시 만든다. dist/에는 실행 시 생기는 config.ini(ERP 토큰 포함)도 들어가므로 더더욱 제외한다.
erp-simulator/build/
erp-simulator/dist/
erp-simulator/dist-new/
erp-simulator/dist-v2/
erp-simulator/*.spec
